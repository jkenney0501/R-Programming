# Installing Libs and packages
# you can install packages by using "Tools" -> "Install Packages" or code below

# ggplot2 - powerful for plots and graphics
# to install
install.packages("ggplot2")

# install xlsx to read excel files
install.packages("xlsx")

# after packages are installed, they need to be loaded.
# look lower left on screen object explore and select "Packages"
# then check the box to load
# OR use library()

library(ggplot2)